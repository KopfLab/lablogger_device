# name of the job
name: Timed Switch Example

# specify which paths to watch for changes
on:
  push:
    paths:
      - examples/timed_switch/*
      - .github/workflows/compile.yaml
      - .github/workflows/compile-timed_switch.yaml

# run compile via the compile.yaml
jobs:
  compile:
    strategy:
      fail-fast: false
      matrix:
        # CHANGE program and specify lib/aux and non-default src as needed
        program:
          - name: 'timed_switch'
            src: 'src'
            lib: 'SDDS SDDS_particleSpike Adafruit_BusIO'
            aux: 'lib/Adafruit_GFX/src/Adafruit_GFX.h lib/Adafruit_GFX/src/Adafruit_GFX.cpp lib/Adafruit_GFX/src/gfxfont.h lib/Adafruit_GFX/src/glcdfont.c lib/Adafruit_SSD1306/src/Adafruit_SSD1306.h lib/Adafruit_SSD1306/src/Adafruit_SSD1306.cpp'
        # CHANGE platforms as needed
        platform:
          - {name: 'p2', version: '6.3.4'}

    # program name
    name: ${{ matrix.program.name }}-${{ matrix.platform.name }}-${{ matrix.platform.version }}

    # workflow call
    uses: ./.github/workflows/compile.yaml
    secrets: inherit
    with:
      platform: ${{ matrix.platform.name }}
      version: ${{ matrix.platform.version }}
      program: ${{ matrix.program.name }}
      src: ${{ matrix.program.src || '' }}
      lib: ${{ matrix.program.lib || '' }}
      aux: ${{ matrix.program.aux || '' }}
